 #include <math.h>
 #include <stdio.h>
int main() {
    int controle;
  int fixonumero = 100;
        for( controle =2; controle <= fixonumero; controle=controle+2)
        {
        printf("%d\n", controle);
      }
    return 0;
}
